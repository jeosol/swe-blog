{{$maxindex := 4}}
{{$datapath :=  .Site.Data.posts.petroleum.watercutpred }}
{{$scenario1 := index $datapath (.Get 0) }}
{{$scenario2 := index $datapath (.Get 1) }}
{{$colheader1 := (.Get 2)}}
{{$colheader2 := (.Get 3)}}
{{$figurenum    := (.Get 4)}}

{{$labels := split (.Get 5) " "}}

<!--
<ul>
{{range $i := (seq 0 $maxindex) }}
  <li>{{index $labels $i}}</li>
{{end}}
</ul>
-->


  {{range $ii,$i := (seq 1 $maxindex) }}
  {{$data1 := index $scenario1 (index $scenario1.cases $i) }}
  {{$data2 := index $scenario2 (index $scenario2.cases $i) }}
  {{$data1fig := (index $data1.satprofiles 0) }}
  {{$data2fig := (index $data2.satprofiles 0) }}
  {{$casename := $data1.casename }}
  <div class="figure-group">
    <div class="two-col">
      <p class="figure-col-header"> {{$colheader1}} </p>
    </div>
    <div class="two-col">
      <p class="figure-col-header"> {{$colheader2}} </p>
    </div>

    <div class= "figure two-col">
      <p class="figure-col-header"> {{$colheader1}} </p>
      <a href={{$data1fig}}><img src={{$data1fig}} alt="Water saturation profiles for {{$casename}} with Kv/Kh = 0.0"/></a>
    </div>
    <div class="figure two-col">
      <p class="figure-col-header"> {{$colheader1}} </p>
      <a href={{$data2fig}}><img src={{$data2fig}} alt="Water saturation profiles for {{$casename}} with Kv/Kh = 0.1"/></a>
    </div>
    <p class="figure-caption">
      Figure {{$figurenum}}({{index $labels $ii}}): Water saturation profile vs. injector-producer distance ({{$casename}}).</p>
    </div>
    {{end}}

